[Unit]
Description=Unbound Exporter for Prometheus
After=network.target

[Service]
ExecStart=/usr/local/bin/unbound-exporter -unbound.ca "" -unbound.cert "" -unbound.host "unix:///run/unbound.ctl"
Restart=always
User=unbound_exporter

[Install]
WantedBy=multi-user.target
